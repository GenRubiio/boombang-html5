import Enum from './Enum';

const CatalogItemTypeOfBehaviorEnum = new Enum({
    NORMAL: 'normal',
    FLYING: 'flying',
    WALKABLE: 'walkable',
    WALKABLE_OVERLAY: 'walkable_overlay',
});

export default CatalogItemTypeOfBehaviorEnum;
