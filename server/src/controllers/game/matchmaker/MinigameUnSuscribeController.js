const Log = require('../../../utils/Log');
class MinigameUnSuscribeController {
    static async main(socket, io, matchMakers, data) {
        try {
          
        } catch (err) {
            Log.error('Error in MinigameUnSuscribeController: ' + err);
        }
    }
}

module.exports = MinigameUnSuscribeController;