const MinigameSceneModel = require('./MinigameSceneModel');

class MinigameRingSceneModel extends MinigameSceneModel {
    constructor(row) {
        super(row); // Llama al constructor de la clase padre
    }

    // Puedes agregar métodos adicionales o sobrescribir los existentes si es necesario
}

module.exports = MinigameRingSceneModel; 